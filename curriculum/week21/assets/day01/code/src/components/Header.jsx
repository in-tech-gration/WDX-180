import "./Header.css";

// 1) Define a Component Function
// 2) (Optionally) import a CSS
// 3) Export the default function

export default function Header(){
  return (
    <header>
      <nav>
        <a href="">Home</a>
        <a href="">Gallery</a>
      </nav>
    </header>
  );
}