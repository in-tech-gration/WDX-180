import {
  whenFormDataChanges,
  readNumber,
  displayString,
} from '../../../../../lib/dom-io/index.js';

whenFormDataChanges('__', () => {
  // debugger;
  console.log('--- form data changed ---');

  // --- read user values ---
  // --- multiply the numbers ---
  // --- create a message ---
  // --- display the message ---
});
