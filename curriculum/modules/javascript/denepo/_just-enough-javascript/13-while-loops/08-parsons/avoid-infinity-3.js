'use strict';


let dots = '';
while (dots.length < 5) {
  dots = dots + '.';
  alert(dots);
}

/* --------- */


dots + '.'; // distractor
while (dots.length !> 5) { // distractor
while (dots.length !< 5) { // distractor
} // distractor
{ // distractor


// #todo
